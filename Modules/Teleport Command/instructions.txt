1. Move "teleport.lua" to "CS2D/sys/lua/hc/modules/teleport.lua"
2. Implement "config.txt" inside "CS2D/sys/la/hc/hc.conf"
	a. Add the "teleport" to the hc.modules table.
	b. Add the permission line at the bottom of the file where applicable.