1. Move "fast_build.lua" to "CS2D/sys/lua/hc/modules/fast_build.lua"
2. Implement "config.txt" inside "CS2D/sys/la/hc/hc.conf"
	a. Add the "fast_build" to the hc.modules table.
	b. Add (and configure) the "hc.FAST_BUILD_CONFIG" where applicable.
